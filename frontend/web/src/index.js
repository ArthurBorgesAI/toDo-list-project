import React from 'react';
import ReactDOM from 'react-dom/client';

import RoutesSolver from './routes';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <RoutesSolver />
  </React.StrictMode>
);
